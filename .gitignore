# Dependencies
node_modules/
# CHANGE: Removed package-lock.json from gitignore to enable npm cache in CI
# WHY: GitHub Actions setup-node with cache:npm requires tracked lock file
# REF: mutation-tests.yml:42 (cache: npm)
# INVARIANT: ∀ workflow w: uses(w, 'cache:npm') → package-lock.json ∈ tracked_files
yarn.lock
pnpm-lock.yaml

# Build output
dist/
build/
*.js
*.js.map
*.d.ts
*.d.ts.map

# Test coverage
coverage/
.nyc_output/

# Mutation testing reports - ignore all except incremental cache
# CHANGE: Track stryker-incremental.json for cross-commit performance optimization
# WHY: Incremental mode reuses ~94% of mutation results between commits (3731/3965 typical)
# QUOTE(User): "я думаю стоит везде сохранять, включая в кодовую базу, чтобы это можно и локально было видеть"
# REF: https://stryker-mutator.io/docs/stryker-js/incremental/
# INVARIANT: reports/stryker-incremental.json ∈ tracked_files → next_run reuses cached_mutants
# COMPLEXITY: Storage O(M) where M = mutant_count, saves O(M·T) execution time
reports/*
!reports/stryker-incremental.json
!reports/.gitkeep

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.local
.env.*.local

# Temporary files
*.tmp
*.temp
.cache/

# TypeScript incremental build metadata
# CHANGE: Ignore .tsbuildinfo files to avoid dirty working tree on release
# WHY: tsc with composite/incremental writes .tsbuildinfo; we now route it to dist/.tsbuildinfo
# REF: tsconfig.src.json "tsBuildInfoFile"
*.tsbuildinfo
dist/.tsbuildinfo
