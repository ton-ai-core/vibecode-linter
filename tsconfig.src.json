{
  // CHANGE: Solution-style build config for source code (src)
  // WHY: Senior practice — separate build profile; root tsconfig references src+test; ESLint uses root
  // QUOTE(ТЗ): "Давать проверяемые решения через формализацию и строгую типизацию"
  // REF: REQ-TS-SOLUTION-STYLE
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    // Output for build
    "outDir": "./dist",
    "rootDir": "./src",
    // CHANGE: Enable composite for project references (solution-style)
    // WHY: Required by TypeScript when this config is referenced from the root tsconfig
    // QUOTE(ТЗ): "Старшие практики: solution-style с references"
    // REF: REQ-TS-SOLUTION-STYLE
    "composite": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "removeComments": false
  },
  "include": ["src/**/*.ts"],
  "exclude": [
    "node_modules",
    "dist",
    "test",
    "**/*.test.ts",
    "**/*.spec.ts"
  ]
}
